#include <iostream>
#include <vector>

using namespace std;

int subarraySum(vector<int> nums, int k) {
    
}

int main() {
    vector<int> nums = {-1,-1,1};
    int k = 1;

    int maxcount = subarraySum(nums,k);
    cout << maxcount << endl;
}