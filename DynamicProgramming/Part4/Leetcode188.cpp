#include <iostream>
#include <vector>

using namespace std;

int maxProfit(vector<int> &prices, int k) {
    
}

int main() {
    int k = 2;
    vector<int> prices = {3,2,6,5,0,3};

    int profit = maxProfit(prices, k);
    cout << profit << endl;
}